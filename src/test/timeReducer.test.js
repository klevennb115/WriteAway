// import timeReducer from '../redux/reducers/timeReducer'

// // counterReducer should initally be 0
// test('timeReducer should initally be 0', () => {
//     const action = { type: 'INCREASE_TIME'};
//     // First argument is the existing state, the second is your action
//     const returnedState = timeReducer(undefined, action);
//     expect(returnedState).toBe(0);
// });